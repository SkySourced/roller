# node packages (only relevant on personal computers)
package-lock.json 
# typescript compiler settings (only usable on personal computers)
tsconfig.json 
# history of code (i have github)
.history/
# photoshop files (using regex to exclude all .psd files in assets folder)
assets/*.psd